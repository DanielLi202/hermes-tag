# Hermes v2026.6.19 installer accepts manifest_version 1; values above 1 are rejected.
manifest_version: 1
name: hermes-tag
label: Hermes Tag
kind: platform
version: 0.3.0
description: Feishu platform override with per-chat Tier-0/Tier-1 context, standing work, and privacy lifecycle controls.
author: lidongyuan
requires_env:
  - FEISHU_APP_ID
  - FEISHU_APP_SECRET
